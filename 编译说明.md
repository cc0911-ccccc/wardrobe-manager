# 衣橱管家（修复版）编译与安装说明

本项目已修复原版 App「按钮点不动 / 自动识别是假数据」的问题，
现在可以**完全离线手动管理衣橱**：添加、编辑、删除、按分类/位置筛选、搜索、
记录每日穿搭，全部可用；AI 自动识别需要自己配置密钥，暂未接入。

下面是把它变成手机能装的 APK 的两种办法。

---

## 办法一：云端自动编译（最省事，推荐）

不用在自己电脑装任何开发工具，利用 GitHub 免费服务器自动出 APK。

1. 打开 https://github.com 注册/登录，点右上角 `+` → New repository，
   仓库名随意（如 `wardrobe-fixed`），选 **Public**，点 Create repository。
2. 把本文件夹（wardrobe-manager）里的所有内容上传到这个仓库
   （GitHub 网页里可直接拖文件，或用 GitHub Desktop）。
   注意：`.github` 文件夹也要一起上传，它包含自动编译配置。
3. 进入仓库的 **Actions** 标签页，若提示启用，点 **I understand... enable**。
4. 点左侧 **Build APK** → 右上角 **Run workflow** → 再次 Run workflow。
5. 等待几分钟（状态从黄色转绿色），点进去下载 **app-release-apk** 这个产物，
   里面的 `app-release.apk` 就是装到手机的文件。
6. 手机允许「安装未知来源应用」后，点开 APK 即可安装。

> 整个过程不需要 Java、Android Studio 或 Flutter，全在云端完成。

---

## 办法二：在自己电脑用 Android Studio 编译

适合你本地已有开发环境，或想自己改代码。

### 准备环境
1. 安装 Android Studio（官网 https://developer.android.com/studio ）。
2. 首次打开会引导安装 Android SDK，全部默认下一步即可。
3. 安装 Flutter 插件：Android Studio → Settings → Plugins → 搜 Flutter 安装。
4. 安装 Flutter SDK：https://docs.flutter.dev/get-started/install （Windows 版），
   解压到某目录，并把 `flutter\bin` 加到系统 PATH。

### 编译
1. 命令行进入本项目目录（wardrobe-manager）。
2. 执行 `flutter pub get`（下载依赖，需联网）。
3. 执行 `flutter build apk --release`。
4. 生成的文件在 `build\app\outputs\flutter-apk\app-release.apk`。

### 真机调试（可选，更快）
手机插电脑、开 USB 调试，执行 `flutter run` 直接装到手机上看效果。

---

## 修复内容清单

- **添加衣服**：改为手动填写（分类 / 类型 / 颜色 / 品牌 / 尺码 / 位置 / 季节 / 风格 / 备注），
  选照片不再强制走已失效的 AI 接口。
- **首页按钮**：「拍照添加」打开添加表单；「找衣服」跳到衣橱搜索；
  「AI搭配 / 衣柜扫描」给出明确提示而非无反应。
- **衣物详情**：「编辑」可改任意字段；「今天穿了」一键记录穿着并 +积分。
- **衣橱页**：筛选、搜索、删除、编辑全部可用（原本就可用，已连通入口）。
- **穿搭记录**：「记录今天」可勾选当天穿的多件衣服并保存。

> 注：AI 自动识别（拍照识别、衣柜扫描、AI 搭配）依赖第三方密钥，
> 原版写死的演示密钥已失效，故本修复版暂未启用，保持手动管理。
